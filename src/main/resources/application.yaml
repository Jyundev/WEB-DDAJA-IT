server:
  port: 8081
  servlet:
    encoding:
      # 한글깨짐 처리 
      force-response: true

# Spring Data Source 설정 
spring:
  profiles:
    active: proud  # default application 파일의 이름
                 # ex application-dev.yml -> dev

# spring doc
springdoc:
  show-login-endpoint: true # security /login 적용 
  api-docs:
    path: /api-docs
    groups:
      enabled: true
  swagger-ui:
    path: /swagger-ui-custom.html
    enabled: true
    groups-order: ASC
    tags-sorter: alpha
    operations-sorter: alpha
    display-request-duration: true
    doc-expansion: none
  cache:
    disabled: true
  model-and-view-allowed: true

  # sql:
  #   init:
  #     mode: always
      
# loging
# race → warn → info → debug → error(적은 로깅) 순
# 개발 서버는 debug, 운영 서버는 info
logging:
  level:
    com.web: debug

#JWT
jwt:
  header: Authorization
  #HS512 알고리즘을 사용할 것이기 때문에 512bit, 즉 64byte 이상의 secret key를 사용해야 한다.
  secret: a15cf4dd194e1bcd6e73cc7de573afdbe3ff6bccbeacbdb3ad7370471e536bddea1fd06a5ed27763b3c893c087eaa52554a7683b40d55ebb42cc9a52b532b43c
  token-validity-in-seconds: 86400  